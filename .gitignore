# macOS
.DS_Store

# secrets / local config — NEVER commit
.env
.env.*
*.key
*.pem
cf_key*
*-pat.txt
*-resend.txt
*-turnstile.txt

# Cloudflare / wrangler
.wrangler/
wrangler.toml.local

# Node (if we ever add tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/
*.swp
*.swo

# Large source/intermediate video — kept on disk, not in git (superseded by the WebGL scene)
holonograph_anim.mp4
holonograph-loop*.mp4

# Source assets (psd, original-resolution png, manuscript drafts) — kept on disk
# locally; the deployed bundle uses the optimized webp at the root.
assets/
